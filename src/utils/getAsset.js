
export function get_asset(fund, net, column){
  const asset = [];
  const category = (((fund[column]) / (fund[net]) ) * 100)
  const other = ( 100 - (forest))
  a.push(other)
  a.push(category)
  return asset;

  }



